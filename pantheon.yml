api_version: 1
web_docroot: true
php_version: 8.3
database:
  version: 10.6

workflows:
  deploy:
    after:
      - type: webphp
        description: "Run drush updatedb, config:import, and cache:rebuild after every deploy"
        script: private/scripts/quicksilver/post_deploy.php
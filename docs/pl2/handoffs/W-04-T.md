# Handoff-T: W-04 — Document / address coupling signals batch

**Date:** 2026-06-12
**Branch:** `aa/pl-neonbyte-W-04-coupling-signals`
**Issue:** `docs/pl2/handoffs/website-audits/issues/W-04-coupling-signals-batch.md`
**Handoff-A reviewed:** `docs/pl2/handoffs/W-04-A.md`
**Handoff-F reviewed:** `docs/pl2/handoffs/W-04-F.md`

---

## A precondition

Confirmed: A returned PASS. No architecture findings; no blocking issues identified by A.

---

## Tier 1 results

| Check | Command / Method | Expected | Actual | Result |
|---|---|---|---|---|
| Cache-clear | ddev drush cr | exit 0 | NOT RUN — ddev offline | NOT RUN (acceptable) |
| HTTP 200 | curl local URL | 200 | NOT RUN — ddev offline | NOT RUN (acceptable) |
| No property/value changes in diff | `git diff HEAD` — strip comment lines, check remaining `+`/`-` lines | Zero non-comment changes | One line: closing `*/` of old replaced comment (not a property/value) | PASS |

**Note on diff stripping:** after removing all lines matching `/*`, ` *`, and `*/` patterns, the only remaining changed line is the closing `*/` of the original two-line comment that was replaced in `primary-menu-wide.css`. No CSS property name, value, selector, or at-rule was added or removed anywhere in the diff.

**Static grep — comments confirmed present:**

| File | Pattern | Line | Result |
|---|---|---|---|
| `region.css` | `Must sit above` | 25 | PASS |
| `primary-menu-wide.css` | `offset-from-header is set at runtime` | 38 | PASS |
| `primary-menu-wide.css` | `best-effort` | 46 | PASS |
| `hero.css` | `intentionally` | 38 | PASS |
| `hero.css` | `must stay in sync` | 42 | PASS |

---

## Tier 2 results

| Check | Method | Result |
|---|---|---|
| Component rendering in SDC styleguide | N/A — comment-only changes; no component structure altered | N/A |
| Heading hierarchy | N/A — no template changes | N/A |
| ARIA landmarks | N/A — no template changes | N/A |
| Semantic structure | N/A — no template changes | N/A |
| Focus order | N/A — no interactive element changes | N/A |

---

## WCAG contrast verification

N/A — no colour, background, or typography values were changed in this phase.

---

## Mobile responsive verification

N/A — no responsive overrides in this phase. F explicitly confirmed this.

---

## Acceptance criteria status

| Criterion | File | Evidence | Result |
|---|---|---|---|
| Finding A: comment above `z-index` explaining what this region must stack above and why 20 was chosen | `region.css` | Line 25: "Must sit above the fixed site header (z-index: 10) and the mobile nav button (z-index: 11). 20 is the next round step above that ladder; do not lower below 12..." | PASS |
| Finding A: lower z-index value to 10–12 if safe without visual regression | `region.css` | Brief made this conditional on visual regression verification; ddev offline; F conservatively kept at 20 and documented. This is the explicitly permitted fallback path in the brief's implementation notes. | PASS (deferred per spec) |
| Finding B: comment documenting that `--offset-from-header` is set by JS | `primary-menu-wide.css` | Line 38: documents `primary-menu.js (init())` as the setter, describes the measurement logic and inline-style delivery | PASS |
| Finding B: comment stating whether 250px is a safe fallback or legacy placeholder | `primary-menu-wide.css` | Lines 45–48: "best-effort approximation, not a guaranteed match; if the header layout changes materially, update the fallback" | PASS |
| Finding B: correct fallback value if wrong | `primary-menu-wide.css` | F reviewed JS source (lines 217–232) and found no more-correct static value; 250px kept; documented as best-effort. No change needed. | PASS |
| Finding C: comment noting `padding-top` and `margin-top` are intentionally paired via `--space-for-fixed-header` and must change together | `hero.css` | Line 38: "padding-top and the parent's negative margin-top are intentionally paired via --space-for-fixed-header... Both values must stay in sync; changing one without the other will cause a gap or an overlap" | PASS |

---

## Blocking issues

None.

---

## Advisory notes

The F handoff notes (and A confirmed) that the changes are unstaged — not committed. O should commit before passing to S. The z-index lowering sub-criterion for Finding A is intentionally deferred to when the ddev environment is back online; a follow-up task should verify whether lowering from 20 to 12 is safe at that time.

---

T complete, no blocking issues. Ready for S.

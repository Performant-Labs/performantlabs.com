# Handoff-T: Phase W-03 - Tokenise hardcoded -150px margin in header-article.css

**Date:** 2026-06-12
**Branch:** `aa/pl-neonbyte-W-03-header-article-margin`
**Issue:** W-03
**Handoff-A reviewed:** docs/pl2/handoffs/W-03-A.md
**Handoff-F reviewed:** docs/pl2/handoffs/W-03-F.md

## A precondition

Confirmed: A returned PASS.

## Tier 1 results

| Check | Command | Expected | Actual | Result |
|---|---|---|---|---|
| ddev cache clear | `ddev drush cr` | Exit 0 | Docker daemon not running — environment offline | NOT RUN |
| HTTP 200 | `curl` to local dev URL | 200 | NOT RUN (Docker down) | NOT RUN |
| Hardcoded -150px absent in CSS file | `grep -r "\-150px" themes/neonbyte/components/header-article/header-article.css` | No output | No output | PASS |
| Custom property declared on correct selector | `grep "header-article-image-offset" ...header-article.css` | Two lines: declaration + calc() | `--header-article-image-offset: 150px;` on line 26 and `margin-top: calc(-1 * var(--header-article-image-offset));` on line 33 | PASS |
| calc() replacement present | Read file directly | `calc(-1 * var(--header-article-image-offset))` | Present at line 33 | PASS |
| Only one file changed (component CSS) | `git diff HEAD --name-only` | `themes/neonbyte/components/header-article/header-article.css` only | Also shows docs/pl2/workflow-ofts.md, scripts/f_agent.py, scripts/ganymede.py, scripts/qwen_agent.py — but these are pre-existing untracked/unstaged pipeline files unrelated to W-03; the diff against HEAD for component files shows only header-article.css changed | PASS |

Note on "only one file changed": `git diff HEAD --name-only` lists five files, but the four pipeline/docs files (workflow-ofts.md, f_agent.py, ganymede.py, qwen_agent.py) are pre-existing working-tree modifications from the pipeline tooling, not part of this phase. The issue brief's single-file constraint applies to W-03 component changes. `git diff HEAD -- themes/` confirms only `header-article.css` is modified under the theme.

## Tier 2 results

| Check | Method | Result |
|---|---|---|
| Variable scoped to correct selector | File read: `--header-article-image-offset: 150px` is declared directly inside `.header-article__image[class]` block, not at `:root` or theme layer | PASS |
| calc() semantics preserve original value | `calc(-1 * 150px)` = `-150px` — visual offset unchanged by default | PASS |
| No syntax errors | File is plain CSS (no SCSS syntax), curly-brace structure is balanced, nesting is valid CSS nesting (img nested inside .header-article__image[class]) | PASS |
| No other component files changed | `git diff HEAD -- themes/` shows single file | PASS |
| Hardcoded -150px absent from .md documentation/component files | `grep -r "\-150px" themes/neonbyte/components/header-article/` returns only header-article.md which contains prose description, not CSS — no CSS file retains the hardcoded value | PASS |

## WCAG contrast verification

N/A — no color or background changes in this phase. No contrast ratios to verify.

## Mobile responsive verification

N/A — no responsive overrides introduced in this phase. No media queries added or modified.

## Acceptance criteria status

| Criterion | Evidence | Result |
|---|---|---|
| A CSS custom property `--header-article-image-offset` is declared in `.header-article__image[class]` defaulting to `150px` | Line 26: `--header-article-image-offset: 150px;` inside `.header-article__image[class]` block | PASS |
| `margin-top: -150px` is replaced with `margin-top: calc(-1 * var(--header-article-image-offset))` | Line 33 in current file; git diff confirms removal of `-150px` and addition of calc() | PASS |
| If existing spacing token equals 150px, use it — otherwise document decision | F documented that closest token `--spacing-xxxl` is 120px, not 150px; new component-scoped variable is correct | PASS |
| No visual change — rendered offset remains 150px by default | `calc(-1 * 150px)` = `-150px`; value is functionally identical | PASS |
| No other files changed (component scope) | Only `themes/neonbyte/components/header-article/header-article.css` modified under `themes/` | PASS |

## Blocking issues

None.

## Advisory notes

- The remaining `-150px` string in `header-article.md` is prose documentation describing the component behavior, not a CSS value. It does not require remediation.
- Docker/ddev was offline during this run. The Tier 1 HTTP/cache checks were NOT RUN. For a pure CSS static refactor with no template changes, the static file checks are sufficient to confirm correctness. If the environment is brought up before S runs, a smoke-check curl is advisable but not a gate for this phase.
